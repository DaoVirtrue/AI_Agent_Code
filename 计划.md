# LLM 平台 · 十层架构 + 三大体系层 · 面试展示版

> 依据：《生产级 AI 原生应用从零搭建完整技术体系 V5 纯净交付版》
> **定位：面试展示项目** —— 体系完整呈现 + 关键链路真实可跑（`docker compose up -d` 一键起）+ 分层清晰可讲。
> 推进方式：分阶段交付，M0 先验证，每阶段独立可验收、可随时叫停。

---

## 一、一句话定位

给面试官讲清楚四件事：**① 十层水平分层（依赖方向）② 三大生产级思考体系（Harness / Herms / RAGAS）③ 代码 MVC 组织清晰 ④ 高并发/亿级的诚实边界（能跑什么、蓝图是什么）**。核心叙事是——"**用工程确定性驾驭模型不确定性**"。

### 1.1 高并发 / 亿级的诚实边界（面试加分项，不是减分项）

**关键认知：高并发/高可用/低延迟/亿级是"运维 + 基础设施"的结果，不是纯代码写出来就成立的。** 面试官最欣赏的是你能分清这个边界，而不是含糊地说"我做到了亿级"。

| 层次 | 内容 | 交付方式 | 单机 docker 验证 |
|---|---|---|---|
| **真实跑（Phase 1 MVP）** | 熔断/限流/降级/负载均衡/分级缓存/异步队列/背压/分层超时重试/无状态水平扩展设计 | 真实代码，能跑 | ✅ 能 |
| **蓝图（Phase 2/3/4）** | 亿级请求、异地多活、多 AZ、分库分表、GPU 集群、KV Cache 分层、混沌演练、全链路压测 | 《亿级演进蓝图》设计文档 + 代码骨架 + 接口 + 监控指标 | ⚠️ 需基础设施，标注清楚 |

> 文档口径：**亿级请求/月 = 万级峰值 QPS**（不是亿级 QPS，文档原文明说"物理不可能"）。四阶段路线中，亿级是 Phase 4，Phase 1 明确是"最小可用 MVP + 范围约束"。

**面试话术**："当前交付的是 Phase 1 MVP（单机可跑可验证的完整机制），但亿级的演进路径我已经设计好了——分库分表、读写分离、向量分片、多 AZ、KV Cache 分层调度、异步削峰，这些在《亿级演进蓝图》里有完整的架构方案和代码骨架，只是真实验证需要真实的多机房基础设施和压测，这在单机 docker 上物理做不到。"

---

## 二、三大生产级思考体系（面试重点讲）

这是本项目区别于普通 LLM demo 的**灵魂**。三个体系独立成层，各有明确的"解决什么问题 / 怎么做 / 为什么"。

### 2.1 Harness 层 —— Agent 安全护栏

> **解决什么**：LLM 是不确定性的，Agent 循环若不套硬护栏，会无限循环、刷爆 Token 成本、执行危险操作。
> **核心思想**：用**代码确定性**去约束**模型不确定性**。Agent 能发挥智能，但每一步都受工程护栏约束。

| 护栏 | 机制 | 面试可讲点 |
|---|---|---|
| 最大迭代 | 硬上限 max_steps | 防无限循环 |
| 成本上限 | Token/金额预算熔断 | 防刷爆账单 |
| 输出校验 | Verify 独立校验器（**禁止 LLM 自判**） | "子 Agent 欺骗主编排器是真实攻击面" |
| 高危工具拦截 | 写操作/不可 undo 操作需审批 | Cancel/Pause 语义分离 |
| 超时熔断 | 分层超时表（内层 < 外层） | MCP 3s < 工具 5s < 子 Agent 60s < 主 Agent 300s |
| 业务事务 | Prepare/Commit/Abort + 幂等锁 + 回滚栈 | 半完成状态脏数据防护 |
| 快照回放 | 每步状态落库，可离线复现 | 断点恢复 + 一致性校验 |

### 2.2 Herms 层 —— 评估驱动的自进化闭环

> **解决什么**：系统上线后如何**越用越好**、安全地迭代，而不是"发布即巅峰"。
> **核心思想**：灰度发布 + 在线评估 + 漂移监控 + 自动调优 + 人类监督，构成一个**自更新闭环**。

**H-E-R-M-S 逐字母拆解**（这是面试时最出彩的一段）：

| 字母 | 含义 | 落地 |
|---|---|---|
| **H** | Human 人类监督 | 人工审批、高风险分人工复核、对抗评估兜底 |
| **E** | Evaluation 评估 | 在线评估 + 六维评估 + RAGAS 分数 |
| **R** | Rollout 灰度发布 | 流量染色(version_set_id/strategy_id) + 多版本并行 + 自动决策 |
| **M** | Monitoring 漂移监控 | 拒答率/引用率/负反馈率 + 行为基线 + 会话风险分 |
| **S** | Self-improvement 自更新 | 失败轨迹回灌记忆 + 自动调参 + 版本回滚 |

> 对应文档风险清单第 7 条"自更新奖励黑客 → 对抗评估 + 人类监督"——这正是 Herms 要防的：**自更新必须有护栏，不能放任系统自我奖励跑偏**。

### 2.3 RAGAS 层 —— RAG 质量评估

> **解决什么**：RAG 检索增强生成，怎么量化"检索准不准、答案忠不忠实"。
> **核心思想**：用标准化指标（不是感觉）度量 RAG 质量，配 golden dataset + 漂移监控。

| 指标 | 度量什么 |
|---|---|
| Faithfulness 忠实度 | 答案是否忠于检索到的上下文（防幻觉） |
| Answer Relevancy 答案相关性 | 答案是否切题 |
| Context Precision 上下文精确率 | 检索到的文档是否相关 |
| Context Recall 上下文召回率 | 相关文档是否都被检索到 |
| Answer Correctness 答案正确性 | 答案是否事实正确（需 ground truth） |

配套：Golden Dataset（≥100 例、污染检查、分层抽样）+ 漂移监控 + 评测可视化（雷达图 + 趋势）。

---

## 三、架构总览

### 3.1 十层水平分层（依赖方向，防循环依赖）

统一 import 为 `from src.<layer>.xxx`。新增 `core/`（最底层公共设施）与 `evaluation/`（评测层）。

```
api → services → domain → [repositories / infra]
                        ↘ agents → mcp → [memory / rag]
                          security / observability / resilience / evaluation 横向
                          core 最底层，被所有层依赖，不依赖任何层
```

### 3.2 三大体系层（垂直横切，物理独立目录）

三大体系**物理上独立成目录**，逻辑上横切多个水平层——这样面试时可以"横着讲十层、竖着讲三体系"。

```
src/
├── harness/          # Agent 安全护栏（循环保护/成本上限/校验/事务/回放）
├── herms/            # 自进化闭环（灰度/在线评估/漂移监控/自更新）
├── ragas/            # RAG 质量评估（5指标/golden/漂移/可视化）
├── api/ services/ domain/ repositories/   # 十层……
├── agents/ mcp/ memory/ rag/
├── security/ observability/ resilience/
├── infrastructure/   # 文档的 infra 层（config/redis/broker/providers）
├── evaluation/       # 六维评估 + 迭代闭环
└── core/             # 异常体系 + 工具契约 + 共享 DTO
```

### 3.3 MVC 组织（代码可读性，一眼能讲）

| MVC | 后端 | 前端 |
|---|---|---|
| **Model**（数据） | `domain/` + `repositories/`（领域模型 + ORM 持久化） | `store/`（Zustand 状态，全部真实 API，无 mock） |
| **View**（展示） | `api/schemas/`（DTO/响应包） | `components/` + `pages/`（React 组件） |
| **Controller**（控制） | `api/routes/` + `services/`（路由 + 业务编排） | `api/` + `hooks/`（axios 层 + 自定义 hooks） |

---

## 四、现状诊断（16 项核心事实）

| # | 发现 | 影响 |
|---|---|---|
| 1 | import 风格三分裂：45 裸导入 + 37 `src.` + rag_system 全相对 | 从未整体 import 通过，重排必统一 |
| 2 | `agent_system`/`ai_gateway`/`rag_system` 三包**零交叉导入** | 重排无业务循环依赖，只集中在公共设施 |
| 3 | `app.py` lifespan 只初始化 db/redis/token/deepseek | agent/gateway/rag 端点全 503 |
| 4 | `rag_system/pipeline.py` 全模拟（检索返回假数据） | RAG 必须真重建 |
| 5 | `MilvusStore` 是真实现但**无实例化点** | Milvus 接线是全新工作 |
| 6 | `config/models.yaml` 死数据；`${JWT}` 占位是字面量 | 配置加载要重做 |
| 7 | `.env` 只有 `DEEPSEEK_API_KEY` 真实；其余全空 | LLM/评测 judge 只能走 DeepSeek |
| 8 | 8 处同名类重复（InjectionDefense/PIIDetector/RAGASWrapper/DriftDetector…） | 重排时收敛 |
| 9 | `health.py:184` 坏导入；`iteration_loop.py` 缺 import | 前置修复 |
| 10 | 测试引用不存在的 `src.agent/gateway/rag`（21 处过期导入） | 测试要重写 |
| 11 | alembic `versions/` 空；表靠 `create_all`；Helm `templates/` 空 | 迁移/K8s 补齐 |
| 12 | docker 缺 Milvus/etcd/MinIO/Prometheus/Grafana | compose 重写 |
| 13 | 前端 `api/` 层在生产代码零引用；后端无 auth/dashboard 路由 | 前端真实化要先补后端 |
| 14 | `POST /v1/rag/evaluate` 会 500（`RAGPipeline` 无 `evaluate`） | 评测后端断裂 |
| 15 | `pyproject.toml` 无 ragas 依赖 → 只能走近似实现 | 真 RAGAS 需加依赖 |
| 16 | 前端 ~5600 行死代码；但 useSSE/recharts/Monaco/markdown 可复用 | 清理 + 复用 |

---

## 五、执行路线（M0 → M6，每阶段独立可验收）

### M0 — 地基修复 + 最小跑通（前置，1 天）
统一 import 根为 `src.`；修坏导入 + 缺 import；删空包；修 lifespan 初始化；写统一 `AgentExecutor` 接口。
**验收**：`pytest` 全绿 + `/health` 200。

### M1 — 十层目录重排 + MVC 骨架（机械搬运 + import 改写）
按映射表物理迁移 + 批量改 import + 抽 BaseTool/exceptions + 建 `core/`/`harness/`/`herms/`/`ragas/` 空骨架。
**验收**：`python -c "import src.api.app"` 成功、无循环导入、目录一眼看清分层。

### M2 — 真实 RAG 链路 + Milvus + BGE-M3
`config` 补 `vector_store_type`；接线 `MilvusStore → DenseRetriever → RAGPipeline`；模拟实现换真实检索/生成（LLM 走 DeepSeek）。
**验收**：上传文档 → 检索返回真实结果。

### M3 — Harness 层 + PEV Agent 编排（核心）
Plan-Execute-Verify 状态机 + **Harness 护栏**（迭代/成本/校验/高危拦截/超时熔断）+ 业务事务 + 快照回放 + 接 ToolRegistry/sandbox/SafetyGuard + 接 MemoryManager（执行前 enrich、执行后回灌）。
**验收**：`/v1/agent/run` 真实多步执行、前端看到 Thought→Action→Observation、护栏生效。

### M4 — 韧性专章（文档第 11 部分）
分层超时表、重试预算+风暴检测、断点恢复+一致性校验、Cancel/Pause 语义、资源 TTL。复用 `ai_gateway/retry.py`。
**验收**：注入超时/中断能恢复。

### M5 — RAGAS 层 + Herms 层 + 可视化（面试亮点）
**RAGAS 层**：给 `RAGPipeline` 加 `evaluate()`；加 `ragas` 依赖；新增评测历史端点；前端评测页（5 指标雷达图 + 逐题明细 + 趋势）。
**Herms 层**：流量染色中间件 + 灰度路由配置 + 在线漂移监控指标 + 会话风险分字段 + 自更新闭环接口（评估→诊断→调优→验证）。
**验收**：跑 golden 集 → 前端看到 RAGAS 真分数 + 历史趋势；染色字段全链路透传。

### M6 — 前端真实化 + docker 全量 + 收尾
前端真实化（P0 auth/dashboard 后端补路由 + chatStore/RAGPage/AgentPage 切真实 API + 复活 Prompts 三件套 + 清 5600 行死代码）；docker 补 Milvus/etcd/MinIO/Prometheus/Grafana；alembic 初始迁移；K8s 数据平面 + Helm templates。
**验收**：`docker compose up -d` 一键起，前端全页面真实化、无 mock，全链路可测。

### M7 — 《亿级演进蓝图》设计文档（面试交付物，非代码）
写一份独立的 `docs/亿级演进蓝图.md`，把 Phase 2/3/4 的架构方案系统化呈现，含：
- **水平扩展设计**：无状态 app + 外部 PostgreSQL/Redis/消息队列（M0-M6 已真实体现无状态化）
- **分库分表 + 读写分离**：SQLAlchemy 分表策略 + 读写路由（代码骨架 + 接口）
- **向量库分片**：Milvus 多分片 + 分租户隔离（骨架）
- **模型层扩展**：多模型路由、KV Cache 四层、队列隔离、Agent 感知路由、预抢占削峰
- **异地多活 + 跨区域调度**：RPO/RTO、数据同步、流量切换（方案）
- **压测方案 + 容量规划**：locust/k6 脚本骨架、QPS 指标、成本归因
- **混沌演练清单**：MCP 超时/向量库宕机/GPU 掉线/RAG 脏数据/Prompt 注入

每项标注【真实可跑】或【需基础设施验证】，面试时诚实讲清楚边界。
**验收**：面试时可照稿讲清"当前 MVP + 亿级演进路径"。

---

## 六、面试叙事提纲（讲系统思考）

> 这是你给面试官讲的主线，每一句都对应代码里真实存在的模块。

1. **开场定位**：这不是一个"调 API 的 demo"，而是一个**分层清晰、可落地、可演进**的生产级 LLM 平台骨架。
2. **水平分层**：十层 + core，讲依赖方向（api→services→domain，domain 不依赖框架，repositories 不穿透 ORM），讲"为什么分层能防循环依赖、防 God Class"。
3. **三大体系**（重点）：
   - **Harness**：用工程确定性驾驭模型不确定性——Agent 智能但每一步受护栏约束。
   - **Herms**：评估驱动的自进化闭环 H-E-R-M-S，系统上线后越用越好、安全迭代。
   - **RAGAS**：用标准化指标量化 RAG 质量，而不是拍脑袋说"检索挺好"。
4. **韧性专章**：分层超时、重试预算、断点恢复、Cancel/Pause、资源 TTL——"任何外部调用必须显式超时，任何重试必须带幂等键"。
5. **可观测 + 成本**：Prometheus/Grafana + 全维度成本计量（不只 Token，还有工具耗时/GPU/向量检索）。
6. **MVC 清晰**：Model/View/Controller 三层一眼能看懂，前端 stores 全部真实 API 无 mock。
7. **高并发/亿级的诚实边界**（收尾亮点）：坦诚讲"当前是 Phase 1 MVP，单机可验证的机制全部真实跑通；亿级是 Phase 4 的演进目标，我已经在《亿级演进蓝图》里设计好了分库分表/多活/GPU 集群/压测方案，真实验证需要真实基础设施"。这一段的"诚实 + 有边界感"最能打动面试官。

---

## 七、关键文件（新建 / 重构代表）

**新建（三大体系）**：
- `src/harness/`：`guard.py`（循环/成本护栏）、`verify.py`（独立校验器）、`transaction.py`（业务事务）、`checkpoint.py`（快照回放）
- `src/herms/`：`grayscale.py`（灰度路由）、`traffic_tagger.py`（流量染色）、`drift_monitor.py`（漂移监控）、`risk_scorer.py`（会话风险分）、`self_improve.py`（自更新闭环）
- `src/ragas/`：`metrics.py`（5 指标）、`golden_dataset.py`（黄金集）、`drift.py`（漂移）、`report.py`（评测报告）
- `src/agents/`：`orchestrator.py`、`state_machine.py`（PEV 状态机）
- `src/resilience/`：`timeout.py`、`retry_budget.py`、`recovery.py`、`reclaimer.py`

**重构**：`src/rag/pipeline.py`（真实检索/生成）、`src/services/model_gateway/router.py`、`src/api/app.py`（lifespan 全量初始化）

**复用不动**：`ai_gateway/` 熔断/降级/负载均衡、`agent_system/memory/` 五层记忆、`security/`、`monitoring/`、`mcp_integration/`、`evaluation/six_dimension.py` + `ragas_wrapper.py`

---

## 八、验证方案（端到端）

1. `docker compose up -d` 一键启动，`/health` 返回 healthy
2. `pytest` 全绿（覆盖率 ≥ 80%）
3. RAG：上传文档 → `/v1/rag/search` 返回真实 Milvus 检索结果
4. Agent：`/v1/agent/run` 真实多步执行 + 前端展示真实轨迹 + Harness 护栏生效
5. RAGAS：`/v1/rag/evaluate` 跑 golden 集 → 前端 5 指标雷达图 + 历史趋势
6. Herms：请求带染色字段 → 灰度路由生效 → 漂移指标上报
7. 韧性：注入 MCP 超时/LLM 429 → 观察重试/降级/断点恢复

---

## 九、风险与依赖

| 风险 | 应对 |
|---|---|
| DeepSeek 是唯一可用 LLM | RAGAS judge / RAG 生成 / Agent 推理都走 DeepSeek；其余 key 后续补 |
| BGE-M3 下载体积大 | 首次 `docker compose up` 慢，需预热 |
| 真 RAGAS 需联网装依赖 | Dockerfile 加依赖；无网则退回近似实现（`use_ragas_lib=False` 保留） |
| 重排是一次性大手术 | M1 批量执行，靠 M0 测试兜底防回归 |
| 范围是数周级工程 | 分阶段交付，M0 先验证，每阶段可叫停 |

---

## 十、执行节奏

按约定**分阶段交付**：
1. 先做 **M0**（地基修复 + 最小跑通），完成后停下验证（`pytest` 绿 + `/health` 200）
2. 确认方向后继续 **M1 → M6**
3. 每阶段独立可验收、可随时叫停

> 本计划文档是后续编码的指导基准，每阶段完成时同步更新进度。

---

## 十一、实际执行进度（2026-09-16 更新）

| 阶段 | 状态 | 交付物 | commit |
|---|---|---|---|
| M0 地基修复 | ✅ 完成 | 统一 import、修 6 个真实 bug、AgentExecutor | `9e4d087` |
| M1 十层重排 | ✅ 完成 | 十层目录 + 三大体系骨架，18/18 模块无循环依赖 | `7c50c03`~`5b1a847` |
| M2 真实 RAG | ✅ 完成 | pipeline 真实化 + DeepSeek 适配器 + hash fallback | `da8e57a` |
| M3 PEV + Harness | ✅ 完成 | PEV 状态机 + 四大护栏组件 | `b6ed5a1` |
| M4 韧性专章 | ✅ 完成 | 分层超时/重试预算/断点恢复/资源TTL | `d14c1ca` |
| M5 Herms + RAGAS | ✅ 完成 | 灰度/染色/漂移/风险分/自更新 + RAGAS 评测 | `59d3cbc` |
| M6 前端真实化 + docker | 🔄 进行中 | Agent页真实化✅ + RAGAS评测页✅ + docker构建中 | `e9ce3a6`~`2731ac4` |
| M7 亿级蓝图 | ✅ 完成 | docs/亿级演进蓝图.md | `d8ee877` |

**测试**：123 单元测试全绿（`pytest --no-cov -m "not api"`）。

**关键成果**：
- 十层架构 + core 层（`src/` 下 18 个模块，无循环依赖）
- 三大生产级思考体系（Harness / Herms / RAGAS）独立成层
- 4 个生产级 bug 修复（circuit_breaker 指标重复注册、request_log ORM 错误、httpx RedirectLoop、Redis 坏客户端）
- 解开 mcp→agents、agents↔memory 两处耦合

**待完成（M6 剩余）**：
- docker compose build 完成（网络慢，去重型依赖后重跑中）
- docker compose up -d 一键启动验证
- 其余前端 mock 页（dashboard/gateway/mcp/prompts/admin）真实化
